#!/usr/bin/env bash
REAL=$(python3 -c "import os,sys; print(os.path.realpath(sys.argv[1]))" "${BASH_SOURCE[0]}")
DIR="$(dirname "$REAL")"
exec "$DIR/../node_modules/.bin/tsx" "$DIR/../src/cli.ts" "$@"
